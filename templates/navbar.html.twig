<div class="bg-dark">
    <div class="container">
        <div class="row">
            <nav class="col my-2 navbar navbar-expand-lg bg-dark navbar-dark">
                <img src="https://www.cirquedusoleil.com/-/media/feature/components/contextualnavigation/logos/cds_brand_darkback.svg?la=en&vs=1&hash=FF961CD9CEDA9B776EE16535290314ECABE28C73" width="40" height="30" alt="Site logo">
                <a class="navbar-title navbar-brand text-warning font-weight-bold ml-3" >Wild Circus</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div id="navbarContent" class="collapse navbar-collapse">
                    <ul class="navbar-nav ml-4">
                        <li class="nav-item active">
                            <a class="nav-link text-warning" href="{{ path('home_index') }}">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-warning" href="{{ path('show_index') }}">Shows</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-warning" href="{{ path('artist_index') }}">Artists</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-warning" href="{{ path('show_prices') }}">Prices</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-warning" href="{{ path('contact') }}">Contact</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav">
                        <li class="nav-item ml-5">
                            {% if app.user %}
                                <a class="btn btn-warning text-dark" href="{{ path('app_logout') }}" role="button">Logout</a>
                            {% else %}
                                <a class="btn btn-warning text-dark mr-3" href="{{ path('app_login') }}" role="button">Login</a>
                                <a class="btn btn-warning text-dark" href="{{ path('app_register') }}" role="button">Register</a>
                            {% endif %}
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </div>
</div>